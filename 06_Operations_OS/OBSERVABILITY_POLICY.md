# Observability Policy

For each production product, define:
- application errors;
- API failures and latency;
- database health;
- background job failures;
- authentication failures;
- key user-journey success;
- deployment markers;
- business-critical calculation anomalies.

A release is not successful solely because deployment completed. Health must remain within defined thresholds during the observation window.
